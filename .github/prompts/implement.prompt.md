# /implement

Use `@/.github/agents/developer.agent.md`.

Implement code changes for one approved implementation plan only.

Rules:
- refuse execution if no approved plan exists
- modify only planned files
- add/update tests required by acceptance criteria
- provide change summary and validation evidence
